name: Archive all sites (Batch 4)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3,15 * * *"

jobs:
  archive-batch:
    name: Archive batch
    uses: palewire/news-homepages/.github/workflows/reusable-archive-batch-workflow.yml@main
    with:
      batch: 4
    secrets: inherit
