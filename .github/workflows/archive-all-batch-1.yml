name: Archive all sites (Batch 1)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 22,10 * * *"

jobs:
  archive-batch:
    name: Archive batch
    uses: palewire/news-homepages/.github/workflows/reusable-archive-batch-workflow.yml@main
    with:
      batch: 1
    secrets: inherit
