name: "Archive: Canada"

on:
  workflow_dispatch:
  schedule:
    - cron: "0 14 * * *"

jobs:
  archive-bundle:
    name: Archive bundle
    uses: palewire/news-homepages/.github/workflows/reusable-archive-bundle-workflow.yml@main
    with:
      bundle: canada
    secrets: inherit
