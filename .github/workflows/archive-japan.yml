name: "Archive: Japan"

on:
  workflow_dispatch:
#  schedule:
#    - cron: "0 1 * * *"

jobs:
  archive-bundle:
    name: Archive bundle
    uses: palewire/news-homepages/.github/workflows/reusable-archive-bundle-workflow.yml@main
    with:
      bundle: japan
    secrets: inherit
