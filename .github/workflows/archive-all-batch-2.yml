name: Archive all sites (Batch 2)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 23,11 * * *"

jobs:
  archive-batch:
    name: Archive batch
    uses: palewire/news-homepages/.github/workflows/reusable-archive-batch-workflow.yml@main
    with:
      batch: 2
    secrets: inherit
