name: Archive all sites (Batch 9)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 5,17 * * *"

jobs:
  archive-batch:
    name: Archive batch
    uses: palewire/news-homepages/.github/workflows/reusable-archive-batch-workflow.yml@main
    with:
      batch: 9
    secrets: inherit
