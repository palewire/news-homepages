name: Archive all sites (Batch 5)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 2,14 * * *"

jobs:
  archive-batch:
    name: Archive batch
    uses: palewire/news-homepages/.github/workflows/reusable-archive-batch-workflow.yml@main
    with:
      batch: 5
    secrets: inherit
