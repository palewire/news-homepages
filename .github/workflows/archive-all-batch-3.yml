name: Archive all sites (Batch 3)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0,12 * * *"

jobs:
  archive-batch:
    name: Archive batch
    uses: palewire/news-homepages/.github/workflows/reusable-archive-batch-workflow.yml@main
    with:
      batch: 3
    secrets: inherit
