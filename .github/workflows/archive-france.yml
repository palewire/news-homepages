name: "Archive: France"

on:
  workflow_dispatch:
  schedule:
    - cron: "0 8 * * *"

jobs:
  archive-bundle:
    name: Archive bundle
    uses: palewire/news-homepages/.github/workflows/reusable-archive-bundle-workflow.yml@main
    with:
      bundle: france
    secrets: inherit
