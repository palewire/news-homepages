name: Archive all sites (Batch 7)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 4,16 * * *"

jobs:
  archive-batch:
    name: Archive batch
    uses: palewire/news-homepages/.github/workflows/reusable-archive-batch-workflow.yml@main
    with:
      batch: 7
    secrets: inherit
