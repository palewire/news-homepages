name: Archive
description: Upload screenshots to an archive.org collection

inputs:
  source:
    description: “The site handle to screenshot”
    required: true
  access-key:
    description: An API key with permission to post to archive.org
    required: true
  secret-key:
    description: An API secret with permission to post to archive.org
    required: true
  collection:
    description: The identifier of the target archive.org collection
    required: true

runs:
  using: "composite"
  steps:
    - id: install
      name: Install Python dependencies
      uses: ./.github/actions/install

    - uses: actions/download-artifact@v3
      with:
        name: screenshots

    - uses: actions/download-artifact@v3
      continue-on-error: true
      with:
        name: accessibility

    - uses: actions/download-artifact@v3
      continue-on-error: true
      with:
        name: hyperlinks

    - uses: actions/download-artifact@v3
      continue-on-error: true
      with:
        name: lighthouse

    - uses: actions/download-artifact@v3
      continue-on-error: true
      with:
        name: wayback

    - id: archive
      name: Archive
      run: |
        mkdir _dist
        pipenv run python -m newshomepages.archive ${{ inputs.source }} > _dist/${{ inputs.source }}.json
      shell: bash
      env:
        IA_ACCESS_KEY: ${{ inputs.access-key }}
        IA_SECRET_KEY: ${{ inputs.secret-key }}
        IA_COLLECTION:  ${{ inputs.collection }}

    - id: save
      name: Save artifact
      uses: actions/upload-artifact@v2
      with:
        name: archive
        path: _dist
        if-no-files-found: error
